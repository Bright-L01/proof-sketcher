<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{ project_name }} - Proof Sketcher</title>
    <link rel="stylesheet" href="css/theorem.css">
</head>
<body>
    <header>
        <h1>{{ project_name }}</h1>
        <p>{{ theorem_count }} theorems documented</p>
    </header>

    <main>
        <h2>Theorems</h2>
        <div class="theorem-list">
            {% for theorem in theorems %}
            <article class="theorem-card">
                <h3><a href="{{ theorem.url }}">{{ theorem.name }}</a></h3>
                <p class="statement">{{ theorem.statement }}</p>
                <p class="summary">{{ theorem.summary }}</p>
                <div class="meta">
                    <span>{{ theorem.step_count }} steps</span>
                    {% if theorem.has_animation %}
                    <span>🎬 Animated</span>
                    {% endif %}
                </div>
            </article>
            {% endfor %}
        </div>
    </main>

    <footer>
        {% if timestamp %}
        <p>Generated on {{ timestamp }}</p>
        {% endif %}
        <p>Created with Proof Sketcher v{{ version }}</p>
    </footer>
</body>
</html>